const tomb =[2,3,4,5,7];

/* const duplatomb = tomb.map(muvelet);

function muvelet(elem){
    return elem*2;
} */
const duplatomb= tomb.map((elem)=>{
    return elem*2;
})


console.log(duplatomb);